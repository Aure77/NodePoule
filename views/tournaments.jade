extends layout

block content
  .main.zerogrid
    - if(tournaments.length===0)
      p Pas de tournois en cours...
    - else
      - each tournament in tournaments
        .competition-list-item
          a(href='/tournaments/#{tournament.tournamentId}')
            span
              | #{tournament.title}
            img(src='/img/slider-img1.jpg', style='width: 100%;')
      div        
        include _paginate